{
  "Name": "Weekly",
  "$Comment": "The weekly warrant board: three heavier contracts that rotate every Monday. 'currencies' is the balance strip in the page header; the weekly contracts pay bounty tokens, so that is the only wallet worth showing here. Add another currency id to the list if you author contracts that pay or spend one.",
  "Payload": {
    "titleKey": "ui.bounty.board.weekly",
    "descriptionKey": "ui.bounty.board.weekly.desc",
    "order": 1,
    "rotation": { "type": "interval", "period": "weekly", "anchor": "utc_monday", "offsetMinutes": 0 },
    "selection": { "type": "weighted_random", "seed": "period" },
    "combatLevelRequirements": { "normal": 25, "hard": 60 },
    "slots": [
      { "filter": { "difficulty": "normal" } },
      { "filter": { "difficulty": "hard" } },
      { "filter": { "difficulty": "hard" }, "optional": true }
    ],
    "currencies": ["bounty_token"],
    "rerollCost": { "currency": "bounty_token", "amount": 100, "maxPerPeriod": 2 }
  }
}
