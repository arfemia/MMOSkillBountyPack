# Patch notes - MMO Skill Bounty Pack

Per-version public release notes (newest first). Same paradigm as the main mod repo: YAML frontmatter (`version`/`type: patch-note`/`status`), user-facing voice, no em-dashes. `CURSEFORGE.md` is the public listing; keep its Versions table in sync.

- [[1.1.1]] - released (requires plugin 1.4.3): contracts always claimed at the board, no lost rewards.
- [[1.1.0]] - released (requires plugin 1.4.0): Daily Contracts + Quick Contracts boards.
- [[1.0.0]] - released (shipped with plugin 1.3.0): first release.
